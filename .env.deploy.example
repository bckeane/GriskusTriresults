# Copy to .env.deploy and fill in your cPanel SSH credentials.
# .env.deploy is gitignored — never commit real credentials.

SSH_HOST=your-server-hostname.com
SSH_USER=your_cpanel_username
SSH_PORT=21098
SSH_KEY=~/.ssh/griskus_deploy
REMOTE_PATH=/home/your_cpanel_username/apps/griskus
